/**
 * ButtonHandler class contains all the functions related to
 * buttons in the game
 */
class ButtonHandler {
	/*
	 * Function that handles event where user pressed play button
	 */
	static handlePlayButton() {
		
	}
	
	/*
	 * Function that handles event where user pressed quit button
	 */
	static handleQuitButton() {
		if (confirm('Are you sure you want to quit the game? :(')) {
			window.close();
		} else {
			
		}
	}
	
	/*
	 * Function that handles event where user pressed switch keys button
	 */
	static handleSwitchKeysButton() {
	}
	
	/*
	 * Function that handles event where user pressed change color button
	 */
	static handleChangeColorButton() {
	}

	/*
	 * Function that handles event where user pressed screenshot button
	 */
	static handleScreenshotButton() {
	}
	
	/*
	 * Function that handles event where user pressed options button
	 */
	static handleOptionsButton() {
	}

}